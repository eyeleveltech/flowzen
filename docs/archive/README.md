# Archive — superseded documents

Everything in this folder is **superseded by `../FLOWZEN-MASTER-PLAN.md`**, which is the single
source of truth. These are kept for history, not for reference — where they disagree with the master
plan, the master plan is right.

| File | What it was | Why it is here |
|---|---|---|
| `SYSTEM-PLAN.md` | First attempt at the system design | Replaced by master plan §3 |
| `JOURNEY.md` | The end-to-end journey, 11 diagrams | Folded into master plan §4 |
| `FOUNDATION.md` | Target schema in Prisma | Folded into master plan §3 |
| `FLOWZEN_FLOW.md` | Early flow notes at the repo root | Folded into master plan §4 |
| `flowzen-journey.drawio` / `.svg` / `.png` | Journey diagram | Describes the **old** system — the pipeline it draws no longer exists |

**Still current, in `../`:** `flowzen-application-flow.*` and `flowzen-system-design.*` — the
draw.io diagrams of the target design.
